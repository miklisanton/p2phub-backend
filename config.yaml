database:
  host: db
  port: 5432
  name: p2p_bot
  user: antonmiklis
  password: ${DB_PASSWORD}
  ssl: disable
redis:
  host: cache
  port: 6379
  db: 0
rabbitmq:
  url: amqp://guest:guest@rabbitmq:5672/
telegram:
  api-key: ${BOT_TOKEN}
  bot-link: t.me/p2pmonitor_bot
exchange:
  max-retries: 20
  retry-delay: 30
website:
  port: 1323
  jwt-secret: ${JWT_SECRET}
